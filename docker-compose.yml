services:
    estoque:
        build: .
        ports:
            - "8501:8501"
        volumes:
            - ./DB:/app/DB
            - ./config.yaml:/app/config.yaml 
        restart: unless-stopped
